pod:
  replicas:
    server: 1

# (optional) keep it tiny to save RAM; bump later if needed
resources:
  enabled: true
  pod:
    requests:
      memory: 128Mi
      cpu: 50m
    limits:
      memory: 256Mi
